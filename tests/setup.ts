import path from 'path';

export const FIXTURES_DIR = path.join(__dirname, 'fixtures');
export const EMPTY_REPO = path.join(FIXTURES_DIR, 'empty-repo');
export const TS_REPO = path.join(FIXTURES_DIR, 'typescript-repo');
export const PY_REPO = path.join(FIXTURES_DIR, 'python-repo');
export const PARTIAL_REPO = path.join(FIXTURES_DIR, 'partial-agentops');
